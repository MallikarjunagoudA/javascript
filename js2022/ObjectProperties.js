// * Object properties

// * we can now use dynamic properties

let myName = "Mallikarjun"

const myObj ={
    [myName]: "hello dear bestie",
    age: 26,
    adress: "somanakatti"
}

// console.log(myObj);

// * No need to write key and value, if both are same.

 let name ="mallikarun"
let age =25

const emp ={
    name,
    age
}
 console.log(emp)